# 📚 Multilingual Evaluation Dataset Generator

This folder contains the full pipeline used to generate the **120-query multilingual evaluation dataset** for:

**“Democratizing Legal Information in India through Multilingual Semantic Retrieval” (2025)**

The script produces realistic English legal queries, translates them into **22 Indic languages**, retrieves top-K matching cases, and exports multiple dataset formats.

---

## 🚀 Features

- Generates **realistic English legal queries** using BART summarization  
- Creates **semantic embeddings** using `all-mpnet-base-v2`  
- Retrieves **top-K relevant cases** with cosine similarity  
- Translates queries into **22 Indic languages** using **IndicTrans2 v2**  
- Outputs JSON, long CSV, wide CSV, and ZIP files  
- Handles large corpora & GPU acceleration  

---

## 📁 Files in This Folder

| File | Purpose |
|------|---------|
| `generate_multilingual_dataset.py` | Main script for creating the multilingual dataset |
| `README.md` | This documentation |

---

## 📦 Output Files Generated by Script

After running the script, you’ll get:

- `Eval_data.json`  
- `Eval_data_long.csv`  
- `Eval_data_wide_ids.csv`  
- `Eval_data_outputs.zip`

These files contain the multilingual queries and their corresponding top-K legal cases.

---

## 🧠 Requirements

- Python 3.9+  
- PyTorch (GPU strongly recommended)  
- `transformers`  
- `sentence-transformers`  
- `accelerate`  
- `scikit-learn`  
- A GPU environment (Kaggle / Colab works perfectly)

---

## ▶️ How to Run

```bash
python generate_multilingual_dataset.py
